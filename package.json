{
  "name": "binary-charts",
  "version": "0.1.2",
  "description": "High-level React chart component that hopefully could be use for binary.com",
  "main": "index.js",
  "scripts": {
    "test": "mocha --compilers js:babel-core/register ./src/**/**--test/*.js",
    "clean": "rm -rf lib",
    "compile": "npm run clean && webpack --config webpack.config.publish.js",
    "prepublish": "npm run compile",
    "ci": "webpack --progress --colors --watch"
  },
  "keywords": [
    "binary.com"
  ],
  "author": "qingwei@binary.com",
  "license": "MIT",
  "dependencies": {
    "deepmerge": "^0.2.10",
    "echarts": "^3.1.1"
  },
  "devDependencies": {
    "babel-core": "^6.5.2",
    "babel-loader": "^6.2.3",
    "babel-preset-es2015": "^6.5.0",
    "babel-preset-react": "^6.5.0",
    "babel-preset-stage-1": "^6.5.0",
    "chai": "^3.5.0",
    "mocha": "^2.4.5",
    "webpack": "^1.12.14"
  },
  "peerDependencies": {
    "react": "^0.14.7",
    "react-dom": "^0.14.7"
  }
}
