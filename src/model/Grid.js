export const createGrid = ({width: width = 700,height: height = 400} = {}) => ({
    left: "2%",
    right: "8%",
    top: "10%",
    bottom: 60,
    width: 'auto',
    height: 'auto',
    show: true,
    containLabel: true,
});
